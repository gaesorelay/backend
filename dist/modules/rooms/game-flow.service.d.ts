import { OnModuleDestroy, OnModuleInit } from '@nestjs/common';
import { RoomsRepository } from './rooms.repository';
import { RoomStatus } from './types/room.type';
import { TimerService } from '../timer/timer.service';
import { GamesService } from '../games/games.service';
import { AiJudgeService } from '../ai-judges/ai-judges.service';
import { VoteOutcome } from '../games/types/vote-outcome.type';
import { RoomStatusSubject } from './room-status.subject';
export declare class GameFlowService implements OnModuleInit, OnModuleDestroy {
    private readonly roomsRepository;
    private readonly timerService;
    private readonly gamesService;
    private readonly aiJudgeService;
    private readonly roomStatusSubject;
    private readonly logger;
    private readonly contexts;
    private readonly aiVotePromises;
    private unsubscribe?;
    constructor(roomsRepository: RoomsRepository, timerService: TimerService, gamesService: GamesService, aiJudgeService: AiJudgeService, roomStatusSubject: RoomStatusSubject);
    onModuleInit(): void;
    onModuleDestroy(): void;
    startGameFlow(roomUuid: string, emitStatus: (status: RoomStatus, durationMs: number, displayStatus?: string) => void, emitVoteResult?: (outcome: VoteOutcome) => void): void;
    private getContext;
    skipPhase(roomUuid: string): Promise<void>;
    prevPhase(roomUuid: string): Promise<void>;
    private scheduleNext;
    resetFlow(roomUuid: string): Promise<void>;
    private handleStatusChange;
    private handleWaiting;
    private restartWaitingFromJudge;
    private handlePlaying;
    private handleVoting;
    private sendVoteResult;
    private handleEnded;
    private startTurnFlow;
    private setRoomStatus;
    private buildAiJudgeScores;
}
